export interface Opportunity {
    oppid: number,
    manager_name: string;
    manager_email: string,
    description: string,
    location: string,
    skills: string,
    min_exp: number,
    demand: number,
    joining_date: Date
}